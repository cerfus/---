# Инструменты промера видео для Tier 1. Вынесены отдельно от рантайма:
# без них система работает, но все Tier 1 признаки честно становятся
# unavailable с причиной extractor_tooling_unavailable.
#
# Версии закреплены намеренно. Другой декодер может прочитать те же кадры
# иначе, поэтому версия библиотеки входит в extractor_version и участвует
# в идентичности признака (см. 0016_feature_extractor_identity.sql).
opencv-python-headless==5.0.0.2
numpy==2.4.6
imageio-ffmpeg==0.6.0
